import type { SortDirection } from 'types/shared';

export const SORT_DIRECTION: Record<string, SortDirection> = {
  ASC: 'asc',
  DESC: 'desc',
};

export const GENDER = {
  FEMALE: 0,
  MALE: 1,
};
